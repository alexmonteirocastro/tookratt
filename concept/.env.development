# Committed local default: snapshot path (no API, no key).
# Do not set VITE_USE_SNAPSHOT here — .env.development outranks .env.local,
# so a committed true would make live mode impossible to opt into locally.
# Unset is already snapshot (loadDemoData treats anything except "false" as snapshot).
VITE_API_BASE_URL=/api
