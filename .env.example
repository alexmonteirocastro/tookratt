QDRANT_URL=https://your-cluster.cloud.qdrant.io
QDRANT_API_KEY=your-qdrant-cloud-api-key
QDRANT_COLLECTION_NAME=JOBS_ON_THE_HUB
QDRANT_DEV_COLLECTION_NAME=JOBS_DEV
# HTTP timeout (seconds) for Cloud Inference queries; qdrant-client default is 5.
# QDRANT_TIMEOUT=30
EMBEDDING_MODEL=intfloat/multilingual-e5-small

# Embedding requires Qdrant Cloud Inference for intfloat/multilingual-e5-small
# (no local FastEmbed fallback). See docs/adr/0014-embedding-model-migration.md.

# Comma-separated browser origins allowed to call the API (CORS). Defaults to the Vite dev server.
# Production chat SPA lives on app.tookratt.com (ADR-0016) — include that origin on Render.
# The marketing apex (tookratt.com) does not call the API, so it does not need CORS.
# CORS_ALLOWED_ORIGINS=http://localhost:5173,https://app.tookratt.com

# Comma-separated bearer tokens for /chat and /jobs/* (ADR-0011). One key per approved user.
TOOKRATT_API_KEYS=your-key-here,another-key-here
# Temporary cutover alias (ALE-168) — remove once TOOKRATT_API_KEYS is set everywhere:
# HUBSTER_API_KEYS=your-key-here,another-key-here

# Browser-reachable API URL for the frontend Docker build (not a Docker-internal hostname).
# Use /api when serving via the frontend nginx proxy (default in docker compose).
# VITE_API_BASE_URL=/api

# Gemini generation (required for /chat when LLM_PROVIDER=gemini, the default)
LLM_PROVIDER=gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
# GEMINI_MAX_RETRIES=3
# GEMINI_BACKOFF_FACTOR=1.0
# GEMINI_TIMEOUT=30.0

# Instant stub answers for UI testing (no Gemini quota, no Ollama latency)
# LLM_PROVIDER=stub

# Local Ollama generation (set LLM_PROVIDER=ollama; GEMINI_API_KEY not required)
# OLLAMA_BASE_URL=http://localhost:11434/v1
# Docker Compose + Ollama on the host (macOS/Windows Docker Desktop):
# OLLAMA_BASE_URL=http://host.docker.internal:11434/v1
# Ollama Cloud (optional; local Ollama needs no key).
# Sign up at ollama.com, create a key, add it to local .env — never commit it.
# OLLAMA_BASE_URL=https://ollama.com
# OLLAMA_API_KEY=
# OLLAMA_MODEL=qwen3:8b
# OLLAMA_TIMEOUT_SECONDS=120.0
# CPU inference: default 60s may 502 with limit=5 context — try 180–300 or lower /chat limit
# OLLAMA_MAX_CHARS_PER_JOB=1200
# OLLAMA_NUM_PREDICT=256

# POST /chat hardening (defaults shown)
# CHAT_QUESTION_MAX_LENGTH=500
# CHAT_RATE_LIMIT=10/minute
# CHAT_SOURCE_MIN_SCORE=0.85  # eval/sweep only; does not gate POST /chat (ADR-0018)

# Grafana Cloud Loki (ADR-0015) — optional; all three required to enable push.
# GRAFANA_LOKI_URL=https://logs-prod-XXX.grafana.net/loki/api/v1/push
# GRAFANA_LOKI_USER_ID=
# GRAFANA_LOKI_API_KEY=

# Optional: outbound Hub API client (defaults shown)
# HUB_CLIENT_MAX_RETRIES=3
# HUB_CLIENT_BACKOFF_FACTOR=1.0
# HUB_CLIENT_REQUEST_DELAY=0.25
# HUB_CLIENT_TIMEOUT=30.0
